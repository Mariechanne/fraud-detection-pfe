# 🕵️‍♀️ Détection de Fraudes Bancaires — Projet de Fin d'Études

Ce projet de fin d'études a pour objectif de concevoir et déployer un système de **détection automatique de transactions bancaires frauduleuses** à l'aide du Machine Learning.  
L'application finale est réalisée en **full Python** avec **Streamlit** pour offrir une interface simple et interactive.

---

## 🎯 Objectifs du projet

- Prétraiter et analyser un **dataset transactionnel** afin d'en comprendre les caractéristiques et le déséquilibre des classes.  
- Entraîner plusieurs modèles supervisés (Logistic Regression, RandomForest, XGBoost).  
- Utiliser des techniques de rééquilibrage (**SMOTE**, **class_weight**, **undersampling**) pour améliorer la détection des fraudes.  
- Optimiser le **Recall (fraudes)** pour minimiser les transactions suspectes non détectées.  
- Évaluer les performances avec des métriques pertinentes (**Recall**, **Precision**, **F1-score**, **PR-AUC**, **ROC-AUC**).  
- Déployer une **application Streamlit** permettant la prédiction sur une transaction ou un fichier CSV.  

---

## 🗃️ Données

- **Source :** [Kaggle – Credit Card Fraud Detection](https://www.kaggle.com/datasets/mlg-ulb/creditcardfraud)  
- **Nombre de transactions :** 284 807  
- **Variables :** 28 features anonymisées (V1…V28), `Amount`, `Time`, `Class`  
- **Classe positive :** `Class = 1` → Transaction frauduleuse  
- **Particularité :** jeu de données **extrêmement déséquilibré (~0.17 % de fraudes)**.  

📁 Les données brutes doivent être placées dans :

```
data/raw/
```

*(Le dossier `data/` est ignoré par Git pour éviter de versionner les fichiers volumineux.)*

---

## 🧰 Outils et technologies

- **Langage :** Python 3.11  
- **Bibliothèques principales :**
  - `pandas`, `numpy` → manipulation et nettoyage des données  
  - `scikit-learn` → pipelines, modèles, métriques  
  - `xgboost` → modèle performant pour données déséquilibrées  
  - `imbalanced-learn` → SMOTE, undersampling, class weights  
  - `shap` → interprétation et importance des variables  
  - `streamlit` → interface web interactive  
- **Autres outils :**
  - `matplotlib`, `plotly` → visualisations  
  - `joblib` → sauvegarde et chargement de modèles  
  - `pytest`, `flake8`, `black`, `isort` → qualité et test du code  

---

## 📁 Structure du dépôt

```
fraud-detection-pfe/
│
├── app/                    # Application Streamlit
├── data/                   # Données locales (non versionnées)
│   ├── raw/                # Données brutes
│   └── processed/          # Données nettoyées
├── models/                 # Modèles sauvegardés (ignorés par Git)
├── notebooks/              # Notebooks d'analyse et d'expérimentation
├── reports/                # Rapports et figures
│   └── figures/
├── scripts/                # Scripts utilitaires
├── src/                    # Code source Python
│   ├── data/               # Prétraitement et split
│   ├── models/             # Entraînement des modèles
│   └── utils/              # Fonctions utilitaires
└── tests/                  # Tests unitaires
```

---

## ⚙️ Installation et exécution

### 1️⃣ Cloner le projet

```bash
git clone https://github.com/Mariechanne/fraud-detection-pfe.git
cd fraud-detection-pfe
```

### 2️⃣ Créer l'environnement virtuel

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

### 3️⃣ Installer les dépendances

```bash
pip install -r requirements.txt
```

### 4️⃣ Vérifier l'environnement

```bash
python scripts/env_check.py
```

### 5️⃣ Lancer l'application (quand prête)

```bash
streamlit run app/streamlit_app.py
```

---

## 📊 Métriques cibles

| Indicateur      | Objectif | Commentaire                                   |
|-----------------|----------|-----------------------------------------------|
| Recall (fraude) | ≥ 0.90   | Prioritaire : minimiser les fraudes non détectées |
| Precision       | ≥ 0.20   | Contrôle du taux de fausse alarme             |
| F1-score        | ≥ 0.30   | Équilibre Recall / Precision                  |
| PR-AUC          | ≥ 0.85   | Plus représentatif pour classes déséquilibrées |
| ROC-AUC         | ≥ 0.95   | Indicateur global                             |

---

## 🧩 Validation méthodologique

- **Validation croisée :** `StratifiedKFold(n_splits=5, shuffle=True, random_state=42)`
- **Anti-fuite :** SMOTE ou scaling uniquement sur train (intégré dans le Pipeline)
- **Sélection du seuil optimal** à partir de la courbe Precision-Recall
- **Interprétation :** graphique SHAP summary plot et features importance

---

## 💻 Déploiement prévu

L'application Streamlit permettra :

- Saisie manuelle d'une transaction (formulaire)
- Téléversement d'un fichier CSV pour prédiction en masse
- Affichage du résultat (fraude / normale) avec probabilité
- Export CSV des prédictions
- (Optionnel) explication des facteurs clés de la décision

---

## 👩🏽‍💻 Auteur & contexte

- **Auteur :** Marie Chandeste Melvina J. H. Medetadji Migan 
- **Formation :** Licence Professionnelle en Data Science pour la Gestion des Entreprises
- **Année académique :** 2024 – 2025
- **Encadrant :** M. DOUMI KARIM / M. KHALID BENABBESS
- **Établissement :** ESLSCA Paris – Campus Rabat

---

## 🗺️ Avancement du projet

- ✅ **Phase 0 :** Cadrage & Setup
- 🕐 **Phase 1 :** Données & EDA
- ⬜ **Phase 2 :** Préparation & Déséquilibre
- ⬜ **Phase 3 :** Modélisation
- ⬜ **Phase 4 :** Validation & Interprétation
- ⬜ **Phase 5 :** Déploiement Streamlit
- ⬜ **Phase 6 :** Rapport final et soutenance

---

## 📜 Licence

Projet réalisé dans un cadre académique — utilisation à but pédagogique uniquement.

---